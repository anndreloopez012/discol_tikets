; register_globals = desactivado
; allow_url_fopen = off

memory_limit = 512M
post_max_size = 190M
file_uploads = Encendido
upload_max_filesize = 190M

; expose_php = desactivado
max_input_time = 120
; variables_order = "EGPCS"
; extension_dir = ./
; upload_tmp_dir = / tmp
; precisión = 12
SMTP = relay-hosting.secureserver.net
; url_rewriter.tags = "a = href, area = href, frame = src, input = src, form =, fieldset ="


; Solo elimine el comentario de las líneas del optimizador zend si su aplicación requiere el soporte de Zend Optimizer

; [Zend]
; zend_optimizer.optimization_level = 15
; zend_extension_manager.optimizer = / usr / local / Zend / lib / Optimizer-3.3.3
; zend_extension_manager.optimizer_ts = / usr / local / Zend / lib / Optimizer_TS-3.3.3
; zend_extension = / usr / local / Zend / lib / Optimizer-3.3.3 / ZendExtensionManager.so
; zend_extension_ts = / usr / local / Zend / lib / Optimizer_TS-3.3.3 / ZendExtensionManager_TS.so


; - ¡Tenga mucho cuidado de no desactivar una función que pueda ser necesaria!
; - Descomente las siguientes líneas para aumentar la seguridad de su sitio PHP.

; disable_functions = "archivo_de_fondo, ini_alter, ini_restore, openlog, passthru,
                      phpinfo, exec, system, dl, fsockopen, set_time_limit,
                      popen, proc_open, proc_nice, shell_exec, show_source, symlink "

extension=php_interbase.dll


[browscap]
browscap = /home/content/b/o/o/boombastic/extra/php_browscap.ini